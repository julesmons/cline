export abstract class FileSystemIntegration {

  // TODO: Use https://code.visualstudio.com/api/references/vscode-api#FileSystemProvider with workspace:// scheme?? Would allow Recline to read and write files in multiple workspaces at once instead of being stuck using the constant... Resulting in safer and more reliable file operations.
}
